La aplicación superturno, Se puede escoger un turno rápido o un turno normal, conectándose 
al servidor parse. Por ahora solo lee un turno y asigna los datos de la fila y el Número del turno.
También tiene integrado la parte de promociones donde se obtienen de un servicio en la nube, dropbox y 
Se van cambiando las promociones cada cierto tiempo.

Se espera poder desde otra aplicación cambiarle los valores para que se le avise a la aplicación cuando 
debe pasar el usuario.

Profe intente que funcionará con el job de parse, pero tenía muchos errores. Así que decidí pasar de
el, para mostrar algo funcional.
